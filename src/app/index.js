import React from 'react';
import { render } from 'react-dom';

import App from './App'


render(<App />, document.getElementById('app')); //Todo el contenido se renderiza en index.html por el ID 'app'